import React from "react";

const Offline = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      You are offline!
    </div>
  );
};

export default Offline;
